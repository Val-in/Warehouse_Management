<Window x:Class="Warehouse_Management_System.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Warehouse_Management_System"
        mc:Ignorable="d"
        FontFamily="Segoe UI"
        Title="MainWindow" Height="300" Width="500">
    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="Black" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="FontSize" Value="16" />
            <Setter Property="HorizontalAlignment" Value="Center" />
            <Setter Property="Margin" Value="5" />
            <Setter Property="Padding" Value="10" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center" Margin="10,0" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="Gold" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <DockPanel LastChildFill="True" Background="#ECF0F1">

        <!-- Toolbar -->
        <ToolBar DockPanel.Dock="Top" Background="#FFFFFF" Height="40" Padding="5" BorderThickness="0">
            <Button Content="Обновить" />
            <Button Content="Создать" />
            <Button Content="Экспорт в Excel" />
            <Button Content="Настройки" />
        </ToolBar>

        <!-- StatusBar -->
        <StatusBar DockPanel.Dock="Bottom" Background="#BDC3C7" Height="25">
            <StatusBarItem Content="Пользователь: admin" />
            <Separator/>
            <StatusBarItem Content="Время: 12:00" />
            <Separator/>
            <StatusBarItem Content="Статус: Подключено" />
        </StatusBar>

        <!-- Main Layout -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Sidebar -->
            <Border Grid.Column="0" Background="Yellow">
                    <StackPanel>
                        <Button x:Name="EmployeeButton" Click="EmployeeButton_OnClick" Content="Сотрудники" Margin="0, 0, 0, 5" Height="20" Background="Gold"/>
                        <Button Content="Заказы" Margin="0, 0, 0, 5" Height="20" Background="Gold" />
                        <Button Content="Товары"  Margin="0, 0, 0, 5" Height="20" Background="Gold" />
                        <Button Content="Склады"  Margin="0, 0, 0, 5" Height="20" Background="Gold" />
                        <Button Content="Уведомления"  Margin="0, 0, 0, 5" Height="20" Background="Gold" />
                    </StackPanel>
            </Border>

            <!-- Content Area -->
            <ContentControl Grid.Column="1" x:Name="MainContentControl" />
        </Grid>
    </DockPanel>
</Window>
